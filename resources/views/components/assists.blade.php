<div class="details-markings-component py-4">
    {{-- @foreach ($employees as $employee)
        <details-markings-component :employee="{{ $employee }}">
            <div slot="form-report">
                <form action="" method="GET" target="_blank">
                    @csrf
                    <button type="submit" class="bg-transparent h-full hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded">
                        Generar reporte  <i class="fa fa-file-pdf-o" aria-hidden="true"></i>
                    </button>
                </form>
            </div>
        </details-markings-component>
    @endforeach --}}
    
</div>